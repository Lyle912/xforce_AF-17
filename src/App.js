/*
This is the file that renders everything to the site
Anything in the return statement will be rendered
To start the site, in the terminal type: `npm install` and then `npm start`
Look up: `React JS` if you are confused by what you are seeing
*/

import './App.css';

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <p>
          AF-17
        </p>
        <p>
          Edit <code>src/App.js</code> and save to reload.
        </p>
      </header>
    </div>
  );
}

export default App;
